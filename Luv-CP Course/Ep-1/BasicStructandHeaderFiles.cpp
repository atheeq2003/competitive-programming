#include<bits/stdc++.h>       // This header file provides all the library files needed for comp prog
using namespace std;
int main(){
    cout << "Hello" << endl;
    cout << sqrt(16) << endl; // As you can see to print sqrt it requires the use of math.h but here it
                              // is not required.
}